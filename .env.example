# Qdrant
QDRANT_URL=http://192.168.86.179:6333
QDRANT_API_KEY=

# Embedding API (base URL; client POSTs to .../v1/embeddings)
EMBEDDING_URL=http://192.168.86.179:30181
# curl: -H "X-Internal-Key: …"
EMBEDDING_INTERNAL_KEY=

EMBEDDING_MODEL=BAAI/bge-m3
VECTOR_SIZE=1024

# Collection suffix for query_chunks: dev | qa | prod
ENV=dev

TOP_K_DENSE=20
RRF_K=60

# Inference / chat API (app.rag_answer; not required for `import app`)
INFERENCE_URL=http://192.168.86.179:30080
INFERENCE_MODEL=Qwen/Qwen2.5-7B-Instruct
# INFERENCE_MAX_TOKENS=512

# Reranker API (base URL; client POSTs to .../v1/rerank)
RERANK_URL=http://192.168.86.179:30182
RERANK_MODEL=BAAI/bge-reranker-v2-m3
# Candidates to rerank from fused retrieval pool
RERANK_TOP_N=50
# Final context passages sent to chat after rerank
FINAL_CONTEXT_TOP_K=10

GRAFANA_CLOUD_URL=https://logs-prod-XXX.grafana.net
GRAFANA_CLOUD_USER=your-stack-user-id
GRAFANA_CLOUD_API_KEY=your-loki-write-token
# If Loki push fails with proxy tunnel errors (e.g. 403), set:
# LOKI_IGNORE_SYSTEM_PROXY=1
